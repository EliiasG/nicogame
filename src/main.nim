import world
import nico
import render/worldrenderer
import test/colliderTest

const orgName* = "EliiasG"
const appName* = "NicoTest"

startColliderTest()
#nico.init(orgName, appName)
#nico.createWindow(appName, 256, 256, 3, false)
#nico.run(gameInit, gameUpdate, gameDraw)